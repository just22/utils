#!/bin/sh

# ----------------------------------------------------------------------
#  $Id$
#
#  Simple timer
# ----------------------------------------------------------------------

# No output msgs, please
exec >/dev/null 2>&1

MENU_ITEMS="\
auto\n\
high"

POL=$(echo -e $MENU_ITEMS |
    dmenu -p "PerfPolicy [$(sysctl -n hw.perfpolicy)]:" "$@")

[ -n "$POL" ] && /usr/bin/doas /sbin/sysctl hw.perfpolicy="$POL"
