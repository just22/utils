#!/bin/sh

# ----------------------------------------------------------------------
#  $Id$
#
#  Simple launcher for vol
# ----------------------------------------------------------------------

if ! pkill -KILL -f "tclsh.*vol"; then ${HOME}/bin/vol "$@"; fi
