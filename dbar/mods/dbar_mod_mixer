#!/bin/sh

# ----------------------------------------------------------------------
#  $Id$
#
#  Mixer level module for dbar
# ----------------------------------------------------------------------

while : ; do
        echo "[Vol: $((100 * $(mixerctl -n outputs.master | cut -d"," -f1) / 255))%]"
        sleep 10
done
