#!/bin/sh

# ----------------------------------------------------------------------
#  $Id$
# ----------------------------------------------------------------------

if ! pkill -f "Xdialog --title Xdialog Calendar"; then Xdialog-calendar; fi
